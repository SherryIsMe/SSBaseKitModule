# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
use_frameworks!

targetArray=['ios','SSBaseKitModule']

targetArray.each do |t|
	target t do
	pod 'SnapKit'

	end
end



